cmake_minimum_required(VERSION 3.10)

set(PROJECT_NAME RectAnnotatorAlpr)
project(${PROJECT_NAME} VERSION 1.0.0)

# tesseract
find_package(raylib CONFIG REQUIRED)

add_executable(${PROJECT_NAME} main.c)
target_sources(${PROJECT_NAME} PUBLIC parson.c parson.h)
target_sources(${PROJECT_NAME} PUBLIC linked_list.c linked_list.h)
target_link_libraries(${PROJECT_NAME} PRIVATE raylib)
# add_custom_command(TARGET ${PROJECT_NAME} POST_BUILD
#     COMMAND ${CMAKE_COMMAND} -E copy_if_different
#         "${CMAKE_CURRENT_SOURCE_DIR}/vosk/libvosk.dll"
#         "$<TARGET_FILE_DIR:${PROJECT_NAME}>"
#     COMMAND ${CMAKE_COMMAND} -E copy_directory
#         "${CMAKE_SOURCE_DIR}/vosk/vosk-model-small-en-us-0.15"
#         "$<TARGET_FILE_DIR:${PROJECT_NAME}>/vosk-model-small-en-us-0.15"
# )
